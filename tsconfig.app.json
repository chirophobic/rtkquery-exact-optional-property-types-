{
  "extends": ["@tsconfig/vite-react/tsconfig.json"],
  "compilerOptions": {
    /*
    Setting this value to true causes RTK Query to incorrectly type the `data`
    property on a query result as possibly undefined, even when the query has
    definitely completed successfully.
    */
    "exactOptionalPropertyTypes": true
  },
  "include": ["src"]
}
